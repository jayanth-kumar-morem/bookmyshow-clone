{
  "name": "@repo/axios",
  "version": "0.0.0",
  "main": "src/index.ts",
  "private": true,
  "scripts": {
    "lint": "eslint . --max-warnings 0",
    "generate:component": "turbo gen react-component",
    "watch": "npx ts-node --transpile-only src/build.ts --watch",
    "build": "npx ts-node --transpile-only src/build.ts"
  },
  "dependencies": {
    "axios": "^1.6.5",
    "rxjs": "^7.8.1"
  }
}
